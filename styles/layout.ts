import { StyleSheet } from 'react-native';

export const layout = StyleSheet.create({
  screen: {
    flex: 1,
    padding: 24,
  },
  center: {
    justifyContent: 'center',
    alignItems: 'center',
  },
  card: {
    borderRadius: 12,
    padding: 24,
  },
});
